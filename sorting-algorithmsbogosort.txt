let bogosort = (v) => {
  let shuffle = (v) => {
  for (let j, x, i = v.length; i; 
  j = Math.floor(Math.random()*i),   
  x = v[--i], v[i] = v[j], v[j] = x);
    return v;
  };
  let isSorted = (v) => {
    for (var i = 1; i < v.length; i++) {
      if (v[i-1] > v[i]) { return false; }
    }
    return true;
  };
  let sorted = false;
  while (sorted == false) {
    v = shuffle(v);
    sorted = isSorted(v);
  } return v; };